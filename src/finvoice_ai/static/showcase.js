function pcmWav(samples, sampleRate = 16000) { const buffer = new ArrayBuffer(44 + samples.length * 2); const view = new DataView(buffer); const write = (offset, text) => [...text].forEach((char, index) => view.setUint8(offset + index, char.charCodeAt(0))); write(0, 'RIFF'); view.setUint32(4, 36 + samples.length * 2, true); write(8, 'WAVE'); write(12, 'fmt '); view.setUint32(16, 16, true); view.setUint16(20, 1, true); view.setUint16(22, 1, true); view.setUint32(24, sampleRate, true); view.setUint32(28, sampleRate * 2, true); view.setUint16(32, 2, true); view.setUint16(34, 16, true); write(36, 'data'); view.setUint32(40, samples.length * 2, true); samples.forEach((sample, index) => view.setInt16(44 + index * 2, sample, true)); return buffer; }
function show(id, data) { document.querySelector(id).textContent = typeof data === 'string' ? data : JSON.stringify(data, null, 2); }
document.querySelector('#analyze-speech').addEventListener('click', async () => { show('#speech-result', 'Analyzing bounded PCM WAV…'); const samples = Array.from({ length: 11520 }, (_, index) => Math.round(1700 * Math.sin(index / 13))); const form = new FormData(); form.append('file', new Blob([pcmWav(samples)], { type: 'audio/wav' }), 'synthetic.wav'); const response = await fetch('/v1/audio/analyze', { method: 'POST', body: form }); show('#speech-result', await response.json()); });
document.querySelector('#run-stream').addEventListener('click', () => { const result = '#stream-result'; show(result, 'Connecting…'); const id = `stream-demo-${crypto.randomUUID()}`; const socket = new WebSocket(`${location.protocol === 'https:' ? 'wss' : 'ws'}://${location.host}/v1/audio/stream/${id}`); const events = []; socket.onmessage = ({ data }) => { const event = JSON.parse(data); if (event.type !== 'accepted') events.push(event); if (event.type === 'finalized') { show(result, events); socket.close(); } }; socket.onopen = () => { [...Array(3).fill(1100), ...Array(15).fill(0)].forEach((value, sequence) => { const pcm = new Int16Array(320).fill(value); socket.send(JSON.stringify({ type: 'audio_chunk', utterance_id: 'guided-demo', sequence, sample_rate_hz: 16000, pcm_s16le_base64: btoa(String.fromCharCode(...new Uint8Array(pcm.buffer))) })); }); }; });
async function tool(name, arguments, confirmed = false) { const response = await fetch(`/v1/demo/tools/${name}`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ arguments, confirmed }) }); show('#tool-result', await response.json()); }
document.querySelector('#read-tool').addEventListener('click', () => tool('get_demo_account_status', { account_id: 'DEMO-001' })); document.querySelector('#write-tool').addEventListener('click', () => tool('create_demo_support_ticket', { session_id: 'browser-demo', category: 'technical' }, true));
